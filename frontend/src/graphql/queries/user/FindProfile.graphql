query FindProfile {
  findProfile {
    email
    username
    displayName
    avatar
    bio
    isTotpEnabled
    notificationSettings {
      siteNotifications
      telegramNotifications
    }
    stream {
      serverUrl
      streamKey
      isChatEnabled
      isChatFollowersOnly
      isChatSubscribersOnly
    }
  }
}
