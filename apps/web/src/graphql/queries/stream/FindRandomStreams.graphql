query FindRandomStreams {
  findRandomStreams {
    id
    title
    thumbnailUrl
    isLive
    tags {
      tag {
        id
        name
      }
    }
    user {
      username
      displayName
      avatar
      id
      isVerified
    }
    category {
      title
      slug
      description
    }
  }
}
