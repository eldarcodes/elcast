query FindAllStreams($filters: FiltersInput!) {
  findAllStreams(filters: $filters) {
    id
    title
    thumbnailUrl
    isLive
    user {
      username
      displayName
      avatar
      id
      isVerified
    }
    category {
      title
      slug
      description
    }
  }
}
