query FindCurrentSession {
  findCurrentSession {
    id
    createdAt
    userId
    metadata {
      ip
      device {
        browser
        os
        type
      }
      location {
        city
        country
        latitude
        longitude
      }
    }
  }
}
