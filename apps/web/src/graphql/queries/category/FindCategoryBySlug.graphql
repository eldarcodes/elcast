query FindCategoryBySlug($slug: String!) {
  findCategoryBySlug(slug: $slug) {
    id
    title
    thumbnailUrl
    description
    createdAt
    streams {
      id
      title
      thumbnailUrl
      isLive
      user {
        username
        displayName
        avatar
        id
        isVerified
      }
      category {
        title
        slug
        description
      }
    }
  }
}
